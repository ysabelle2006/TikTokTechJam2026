"""
Required deliverable script: takes an image directory, runs the
detector over every image, and writes a JSON file of
{"image_path": ..., "pred": ...} records.

TODO (once Detector.predict works): implement the directory walk +
JSON writer described in the brief's deliverables section.
"""


def main(image_dir: str, output_json: str):
    raise NotImplementedError


if __name__ == "__main__":
    raise NotImplementedError("wire up argparse once Detector exists")
